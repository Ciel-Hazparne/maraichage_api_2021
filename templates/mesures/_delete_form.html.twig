<form method="post" action="{{ path('mesures_delete', {'id': mesure.id}) }}"
      onsubmit="return confirm('Voulez-vous supprimer cette ligne?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mesure.id) }}">
    <button class="btn btn-danger">Delete</button>
{#  on supprime la table dans phpmyadmin #}
</form>
